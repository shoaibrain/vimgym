[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "vimgym"
version = "0.2.0"
description = "Local, redacted Claude Code and Codex session search and backup"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}
authors = [{name = "Shoaib Rain"}]
keywords = ["claude", "claude-code", "codex", "ai", "session", "memory", "search", "developer-tools", "local-first"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Environment :: Web Environment",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: MacOS",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Software Development",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Utilities",
]
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.29",
    "watchdog>=4.0",
    "httpx>=0.27",
    "rich>=13",
]

[project.optional-dependencies]
dev = [
    "pytest>=8",
    "pytest-asyncio>=0.23",
    "pytest-timeout>=2.3",
    "httpx",
    "ruff",
    "mypy",
    "build",
    "twine",
    "pytest-cov",
    "pip-audit",
]
browser = [
    "playwright>=1.50",
]

[project.urls]
Homepage = "https://vimgym.xyz"
Repository = "https://github.com/shoaibrain/vimgym"
Issues = "https://github.com/shoaibrain/vimgym/issues"
Changelog = "https://github.com/shoaibrain/vimgym/blob/main/CHANGELOG.md"

[project.scripts]
vg = "vimgym.cli:main"

[tool.setuptools.package-dir]
"" = "src"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
vimgym = [
    "ui/index.html",
    "ui/style.css",
    "ui/app.js",
    "defaults/redaction-rules.json",
    "defaults/config.json",
]

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.coverage.run]
branch = true
source = ["vimgym"]

[tool.coverage.report]
show_missing = true
skip_covered = false
